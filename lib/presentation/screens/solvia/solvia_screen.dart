import 'package:flutter/material.dart';

class SolvIAScreen extends StatelessWidget {
  const SolvIAScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("SolvIA"),
      ),
    );
  }
}
